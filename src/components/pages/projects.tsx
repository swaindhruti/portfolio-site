import ProjectContainer from "../features/projectsPage/projectsContainer";

const ProjectsPage = () => {
  return (
    <div className="flex flex-col items-center justify-center">
      <ProjectContainer />
    </div>
  );
};

export default ProjectsPage;
